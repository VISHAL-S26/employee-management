apiVersion: v1
kind: Service
metadata:
  name: employee-management-service
spec:
  type: NodePort
  selector:
    app: employee-management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
